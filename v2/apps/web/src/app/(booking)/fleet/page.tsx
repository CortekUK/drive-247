import { PageStub } from "../_stub/page-stub";

export const metadata = { title: "Fleet and Pricing" };

export default function FleetPage() {
  return (
    <PageStub
      title="Fleet and Pricing"
      description="Browse the full lineup with transparent daily, weekly and monthly pricing."
    />
  );
}

import { PageStub } from "../_stub/page-stub";

export const metadata = { title: "About" };

export default function AboutPage() {
  return (
    <PageStub
      title="About Drive247"
      description="The story, the standards, and the people behind every digitally certified rental."
    />
  );
}

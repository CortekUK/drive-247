/**
 * The standard Next.js 404, rendered INSIDE this group's layout (header and
 * footer kept). This is what the design showed on Next 16, where it was built;
 * the booking app runs Next 15, whose default would drop the layout and show a
 * bare page. Same component Next uses for its own default.
 */
export { default } from "next/dist/client/components/builtin/not-found";
